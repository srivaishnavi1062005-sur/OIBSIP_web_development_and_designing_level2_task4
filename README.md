

# 🔐 Super Saver — Login Page

This is a sleek and responsive login interface for the **Super Saver** web application. Designed with Bootstrap and custom styling, it offers a clean user experience with intuitive form elements and iconography.

## ✨ Features

- **User Authentication Form**:
  - Fields for **Username** and **Password**
  - POST method to `/login` for backend integration
- **Bootstrap Icons**:
  - Person and lock icons for visual clarity
- **Responsive Layout**:
  - Centered form using Flexbox (`d-flex flex-column justify-content-center`)
- **Navigation Prompt**:
  - Link to the Sign-Up page for new users

## 🧩 Technologies Used

- HTML5 & CSS3
- Bootstrap 4.4.1
- Bootstrap Icons 1.11.1
- jQuery & Popper.js (for Bootstrap JS functionality)

## 📁 File Structure

- `index.html`: Login page markup
- `styles.css`: External stylesheet for custom styling
- `script.js`: Optional JavaScript for future enhancements

## 🚀 How to Use

1. Clone or download the repository.
2. Ensure `styles.css` and `script.js` are in the same directory.
3. Open `index.html` in a browser.
4. Enter credentials and click **Login**.
5. Use the **Sign Up** link to navigate to the registration page.

## 💡 Notes

- Backend integration is expected at `/login` endpoint.
- Customize `styles.css` to match your brand theme.
- Extend `script.js` for validation, animations, or API calls.

